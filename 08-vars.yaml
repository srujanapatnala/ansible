- name: inventory variables
  hosts: local
  connection: local
  tasks:
    - name: print variables
      vars:
        COURSE: Ansible
      ansible.builtin.debug:
        msg: "course is:{{ COURSE }}"  
