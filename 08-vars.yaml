- name: play level variables
  hosts: local
  connections:
  tasks:
    - name: print variables
      vars:
        COURSE: Ansible
      ansible.builtin.debug:
        msg: "course is:{{ COURSE}}"  
